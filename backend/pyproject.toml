[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.ruff.lint.isort]
known-first-party = ["main", "ai_advisor", "auth", "data_loader", "db", "forecaster", "inventory_logic"]
